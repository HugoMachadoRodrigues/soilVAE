# Predict y using VAEReg (via latent z -\> y_head)

Predict y using VAEReg (via latent z -\> y_head)

Predict y using VAEReg (via latent z -\> y_head)

## Usage

``` r
vae_predict(model, X)

vae_predict(model, X)
```

## Arguments

- model:

  Python VAEReg

- X:

  matrix

## Value

numeric vector
