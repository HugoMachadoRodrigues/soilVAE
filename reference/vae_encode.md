# Extract latent embeddings (z) from VAEReg

Extract latent embeddings (z) from VAEReg

Extract latent embeddings (z) from VAEReg

## Usage

``` r
vae_encode(model, X)

vae_encode(model, X)
```

## Arguments

- model:

  Python VAEReg

- X:

  matrix

## Value

matrix
